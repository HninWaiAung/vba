Sub Sample2()
    Dim buf As String, n As Long
    Open "C:\ƒjƒC\testxml\test.xml" For Input As #1
        Do Until EOF(1)
            Line Input #1, buf
            n = n + 1
            Cells(n, 1) = buf
        Loop
    Close #1
End Sub
